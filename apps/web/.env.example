# Drizzle
DATABASE_URL="postgres://root:mysecretpassword@localhost:5432/local"

# Database connection used by pg-pool-based scripts (e.g. seed)
DATABASE_USER="root"
DATABASE_PASSWORD="mysecretpassword"
DATABASE_HOST="localhost"
DATABASE_PORT="5432"
DATABASE_DB="local"

ENV="local"

ORIGIN=""

# Better Auth
# For production use 32 characters and generated with high entropy
# https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET=""

# Cron endpoint secret — set this in Coolify and match in the scheduled task
CRON_SECRET=""

# Redis cache (optional — set USE_REDIS_CACHE=true to enable)
USE_REDIS_CACHE="false"
REDIS_URI="redis://localhost:6379"

# Admin user seed credentials (required for seed)
ADMIN_SEED_EMAIL="admin@historytimeline.local"
ADMIN_SEED_PASSWORD="changeme-admin-2025"

# Coolify scheduled task setup (Issue 10):
# Add a Scheduled Task in Coolify pointing to your deployed service.
# Command (runs daily, e.g. 0 2 * * *):
#   curl -s -o /dev/null -w "%{http_code}" \
#     -X GET https://your-domain.com/api/cron \
#     -H "Authorization: Bearer $CRON_SECRET"
# The CRON_SECRET value must match the env var set in your Coolify service.
